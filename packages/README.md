# Packages

`packages` contains reusable or publishable artifacts. Packages have explicit
versioning and must not become a shortcut around module boundaries.

The first packages are the TypeScript SDK and language-neutral shared contract
models generated or verified from `contracts/`.
