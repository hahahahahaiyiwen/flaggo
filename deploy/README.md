# Deployment

Deployment assets support local and container-based execution of Flaggo
applications.

The initial Compose configuration runs the fixture-backed contract API. Future
service images must remain independently deployable and use environment
configuration without committing credentials.
