services:
  mock-api:
    build:
      context: .
      dockerfile: deploy/mock-api/Dockerfile
    environment:
      FLAGGO_HOST: 0.0.0.0
      FLAGGO_PORT: 8080
    ports:
      - "${FLAGGO_PORT:-8080}:8080"
